apiVersion: apps/v1
kind: Deployment
metadata:
  name: cie-web-next-deployment
  labels:
    app: cie-web-next
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cie-web-next
  template:
    metadata:
      labels:
        app: cie-web-next
    spec:
      containers:
      - name: cie-web-next
        image: vengelmann/cie-web-next
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: cie-web-next-service
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    app: cie-web-next